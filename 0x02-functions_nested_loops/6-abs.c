#include "main.h"
/**
 * _abs - a function that gets absolute value of an integer
 * @x: a function parameter
 *
 * Return: int
 */
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
